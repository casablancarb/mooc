= form_for [:admin, @exercise, @question] do |f|
  = render 'shared/form_errors', model: @question
  .form-group
    = f.label :body
    = f.text_area :body,
      class: 'form-control'
  .form-group
    = f.submit 'Save',
      class: 'btn btn-primary'
