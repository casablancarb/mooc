.container
  .page-header
    %h1
      Leave feedback
      %br
      %small
        = @exercise.title

  %p
    By leaving constructive feedback, your teacher can adapt the course material to better suit your needs. So please do be honest! Your ideas make this course better!
    %span.text-info
      Your feedback will be totally anonymous.

  = form_for [@exercise, @feedback], url: exercise_feedback_index_path do |f|
    = render 'shared/form_errors', model: @feedback
    .form-group
      = f.label :body
      = f.text_area :body,
        rows: 6,
        class: 'form-control'
    .form-group
      = f.label :rating, 'Rate this exercise'
      = f.select :rating,
        options_for_select([nil, ['4: Very good', 4],['3: Good', 3],['2: Not so good', 2],['1: Terrible', 1]]),
        {},
        class: 'form-control'
    .form-group
      = f.submit 'Submit feedback',
        class: 'btn btn-primary form-control'

